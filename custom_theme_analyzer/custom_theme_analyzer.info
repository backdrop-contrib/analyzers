
name = Custom Theme Analyzer
description = Displays a list of modules which override the system default theme sorted by module's call-up.

package = Analyzers
type = module
backdrop = 1.x

configure = admin/structure/layouts

dependencies[] = block
